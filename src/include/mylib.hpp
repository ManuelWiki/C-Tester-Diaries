// This is the broken function we're testing.
// It's supposed to check if a number is even or not,
// but if you try passing '5', it returns 'true' as if it were an even number
bool broken_is_even(int);